# Workflow template samples

Reference exports from Agent Studio (for format validation only).

- `calculator_workflow_template.zip` — pre-packaged Calculator Workflow template structure (`workflow_template.json` + `studio-data/tool_templates/`).

Generate the Hybrid RAG template locally:

```bash
python scripts/collated_to_workflow_template.py
# writes dist/hybrid_rag_agentic_workflow_template.zip
```

See [docs/WORKFLOW_TEMPLATE.md](../docs/WORKFLOW_TEMPLATE.md) for import instructions.
