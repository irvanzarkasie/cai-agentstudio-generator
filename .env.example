# Copy to .env and fill in (do not commit .env)

# Cloudera AI Workbench base URL (no trailing slash)
CAI_WORKBENCH_HOST=https://ml-1e596f2f-177.irz-tste.a465-9q4k.cloudera.site

# Agent Studio application URL (no trailing slash)
AGENT_STUDIO_URL=https://cai-agent-studio-svf4oc.ml-1e596f2f-177.irz-tste.a465-9q4k.cloudera.site

# CML API v2 key (User Settings → API Keys)
CDSW_APIV2_KEY=

# OpenAI API key (for llm_config at deploy time)
OPENAI_API_KEY=

# GitHub repo URL for workflow source (HTTPS or git@...)
GITHUB_WORKFLOW_URL=https://github.com/irvanzarkasie/cai-agentstudio-generator.git

# Optional: upstream corpus root for scripts/bundle_hybrid_data.py --source
# HYBRID_RAG_SOURCE=/path/to/generative_ai_design_patterns
